#include<stdio.h>

int main(){
    int i=10;
    int j=i;
    int a=2, b=3,c=5,d=4;
    //%d is a format specifier for integer
    // %f is float specifier for float
    printf("the value of i is %d and the value of j is %d\n",i,j);
    printf("the value of a is %d and the value of b is %d\n",a,b);
    printf("the value of is is %d and the value of j is %d\n",c,j);

     return 0;
}